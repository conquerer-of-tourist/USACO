{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/a.out",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build (clang++)",
      "args": [],
      "stopOnEntry": false,
      "console": "integratedTerminal"
    }
  ]
}
