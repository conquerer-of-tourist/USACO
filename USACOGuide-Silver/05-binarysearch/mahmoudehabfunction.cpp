/*

    August 12, 2026 - Warmups Sessions
    hangboy
    Codeforces - E. Mahmoud and Ehab + Function

*/

#include <bits/stdc++.h>
using namespace std;

int n, m, q;

int main()
{
    cin >> n >> m >> q;
    vector<int> a (n), b (m);
    for (auto& val : a) {
        cin >> val;
    }
    for (auto& val : b) {
        cin >> val;
    }
}